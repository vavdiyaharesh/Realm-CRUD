# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm-CRUD' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Realm-CRUD
 pod 'RealmSwift'

  target 'Realm-CRUDTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'RealmSwift'
  end

  target 'Realm-CRUDUITests' do
    # Pods for testing
    pod 'RealmSwift'
  end

end
